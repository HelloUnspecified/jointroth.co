// No content collections yet — the Troth waitlist landing page is a single
// interactive component. Add collections here (e.g. blog, changelog) as the
// site grows.
const collections = {};

export { collections };
